// 0から1までのランダムな整数を生成
var randomNumber = Math.floor(Math.random() * 1);
console.log(randomNumber);

Quantity = float(weight / FruitsWeight[randomNumber]);

var Quantity = sessionStorage.getItem("Quantity");
console.log("保存された値: " + Quantity);